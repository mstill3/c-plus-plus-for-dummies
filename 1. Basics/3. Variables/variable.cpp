#include <iostream>
using namespace std;

int main() 
{
    int x = 8; // define a variable named x, of type int
    double width = 8.8; // define a variable named width, of type double

    int a, b; // define (instantiate) multiple variables
    // random what we'll get, because a and b are uninitialized

    cout << x;
    cout << width;
    cout << a;
    return 0;
}
